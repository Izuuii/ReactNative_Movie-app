{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // ensures paths work correctly
    "paths": {
      "@/*": ["./*"]
    },
    "types": ["nativewind/types"] // 👈 Ensures NativeWind types are loaded
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ]
}